import { createTheme } from "@material-ui/core/styles";

const themeMui = createTheme({
  palette: {
    primary: {
      light: "#ffddc1",
      main: "#ffab91",
      dark: "#c97b63",
      contrastText: "#a98274",
    },
  },
});

export default themeMui;
